const sharedRoles = ["administrator", "Client Admin", "Client Case Manager", "Client Finance Manager", "Client Accountant"];

export const roleConfig = {
  Dashboard: sharedRoles,
  ActiveTimer: sharedRoles,
  AddClientUser: sharedRoles,
  AddFund: sharedRoles,
  DefaultGroup: sharedRoles,
  AllocateCase: sharedRoles,
  AddUser: sharedRoles,
  ReviewInvoice: sharedRoles,
  BasicFilterDemo: sharedRoles,
  AutoChargeHistory: sharedRoles,
  ProjectTrackerRecord: sharedRoles,
  FullCalendar: sharedRoles,
};
